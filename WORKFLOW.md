# VS Code + Android Studio কাজের ধাপ

## 🎯 আপনার Daily Workflow:

### ধাপ ১: কোডিং (VS Code এ করুন)
```bash
# VS Code এ প্রজেক্ট খুলুন
code C:\Users\Arafat\Downloads\CampusFind\CampusFind

# কোড লিখুন, edit করুন, Git operations করুন
```

### ধাপ ২: Build & Run (Android Studio এ করুন)
```bash
# Android Studio খুলুন এবং প্রজেক্ট খুলুন
# Background এ রান করুন
```

### ধাপ ৩: Testing
```bash
# Android Studio এ:
- Emulator create করুন
- App run করুন
- Test করুন
```

## 📁 File Management:
- **VS Code এ**: সব `.java`, `.xml`, `.gradle` files edit করুন
- **Android Studio এ**: শুধু build এবং run করুন

## 🔄 সিঙ্ক রাখার উপায়:
```bash
# VS Code এ কোড change করার পর:
1. Save করুন (Ctrl+S)
2. Android Studio এ যান
3. প্রজেক্ট sync করুন (automatic হবে)
4. Build করুন
```

## ⚡ Quick Commands:

### VS Code এ:
- `Ctrl+P` - Quick file open
- `Ctrl+Shift+F` - Search in files
- `Ctrl+G` - Go to line
- `Ctrl+`` - Terminal

### Android Studio এ:
- `Ctrl+F9` - Build
- `Shift+F10` - Run
- `Ctrl+Shift+A` - Action search
- `Alt+1` - Project view

## 🎨 Best Practices:
1. ✅ কোডিং VS Code এ করুন (fast & lightweight)
2. ✅ Build Android Studio এ করুন (reliable)
3. ✅ Git operations VS Code এ করুন (better UI)
4. ✅ দুটি tool একসাথে open রাখুন
5. ✅ VS Code save করলে Android Studio auto-detect করবে